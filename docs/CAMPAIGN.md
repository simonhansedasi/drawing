# Visualizing the Unseen — LinkedIn campaign

Posts every 3-5 days (changed from daily 2026-08-15 — see Distribution note below). Each post links to the notebook that produced it. Update `status` as pieces post/build.

## Structure

**2026-08-20: Simon dropped the pre-scheduled system going forward** — "forget the schedule, they're just coming out as they come now." Day numbers from here on are whatever day a piece actually posts, not a pre-planned slot; don't enforce the old parity rule or thread assignment on new pieces, and don't invent a day number for something not yet posting. The history below (through Day 11 and the two now-superseded rows noted inline) stays as the record of what was posted/planned under the old system.

**Parity was the hard rule through Day 11:** odd days = whimsical hobby projects, even days = serious civic analysis. This was fixed by what had already posted (Day 1 art, Day 2 civic, Day 3 hobby-math).

Within that, two content threads plus a filler bucket:

- **A-story (even days)** — civic/community-burden data analyses (snotrac transit gap, DOH environmental burden, housing cost). This is the through-line meant to get noticed by employers — sourced public data only, no invented numbers (see workspace `feedback_pipeline_sourced_numbers`).
- **B-story (odd days)** — the Benford's-law-in-language sub-series (`benford_law/lingua`). Seven parts already built end-to-end (01_bounds → 07_spread_gini), each building on the last's finding. The `benford_base26_handoff.md` sequencing note says to run these as a themed run rather than scattering them — do that: consecutive odd days once it starts, not interleaved with filler.
- **Filler (odd days)** — one-off generative-art / score-visualization pieces (Ode to Joy, storm, gallop, 1812, phase space, quasicrystal, interference). No continuity required between them; used to open the series and to fill odd days once B-story is exhausted.

## Format
"Day X of Visualizing the Unseen: [title]" + 1-2 sentence hook + notebook link. **From Day 5 onward**, title carries a `[until I get hired]` tag (Simon's call, 2026-08-13) — Days 1-3 posted without it, don't retrofit those, but use it in every new post going forward.

**From Day 7 onward, no bar/dot/pie charts** (Simon's call, 2026-08-14: "think outside the box"). The `benford_law/lingua` sub-series' visual identity going forward is the theta circle itself — words/data placed at their real angular position on a unit circle, not abstracted into a conventional chart form. `theta_circle.ipynb` established the technique (already posted); Day 7's word clouds and Day 9's core-vs-tail circles extend it. Established B-story pieces built before this call (03_register's line chart, 04_languages' dot chart, 05-08's line/bar charts) aren't retrofitted, but neither are they used for new day slots going forward — see Day 7 and Day 9 rows below for what replaced them.

## Calendar

| Day | Thread | Title | Notebook | Status | Hook |
|-----|--------|-------|----------|--------|------|
| 1 | Filler | Ode to Joy, as terrain | ode_to_joy_landscape.ipynb | POSTED 2026-08-09 | 5 layers (bass/bassoon+viola/violin/tutti), each a real melody-driven waveform; dynamics verified against the IMSLP score (p/p/p dolce/f, not the assumed pp/p/mf/ff); caption in `ode_to_joy_post.md`. |
| 2 | A-story | The commute you can't see | transit_gap_visible.ipynb | POSTED 2026-08-10 | Split snotrac's own model into its two inputs for 575 Snohomish County block groups: who needs transit (vulnerability) vs. what service actually reaches them (frequency-weighted accessibility, near-zero outside I-5/Everett — ~70% of block groups score under 0.05). Composite gap score lands darkest exactly where snotrac's own priority ranking independently flags: Everett, Stanwood, Darrington. |
| 3 | B-story (1/7) | The alphabet was built for this | ../benford_law/lingua/notebooks/01_bounds.ipynb | POSTED 2026-08-11 | Words as bijective base-26 numbers (a=1...z=26). Sort the alphabet by letter frequency and English nearly obeys Benford's law by construction (𝓡=0.237); sort it backwards and it's as far as this corpus gets (0.766). The alphabet we actually inherited lands at 0.309 — far closer to the friendly end than a random relabeling (median 0.47). Caption in `benford_law/lingua/01_bounds_post.md`. |
| 4 | A-story | Who breathes it in | envburden_snohomish.ipynb | READY (4 panels) | PM2.5, diesel/air toxics, heavy traffic, wildfire smoke — 4 individual burden layers for Snohomish County (WA DOH EHD Map v3), then DOH's own combined score. **Corrected 2026-08-11**: it's not a 4-way convergence — PM2.5 and wildfire smoke never reach the state's top decile anywhere in the county; the worst-composite tracts (Mill Creek to Everett to Marysville/Arlington) score high almost entirely on traffic (mean 9.3 vs 6.6 county-wide), with the other three layers near-average even there. **Panel C** (2026-08-11): per-tract dominant-hazard map — wildfire smoke owns the forested east, traffic/diesel own the urbanized I-5 corridor. **Panel D** (2026-08-12): population density reference — traffic/diesel tracts run 9-11x denser than smoke tracts, but density barely moves the composite score itself (rho=0.17). Caption in `envburden_snohomish_post.md`. |
| 5 | B-story (2/7) | Watch a dictionary lose its accent | ../benford_law/lingua/notebooks/02_cascade.ipynb | READY | Turn each word into a number, multiply n random words' numbers together, look at the leading digit of the product. n=1 shows English's own fingerprint (too many s/p/m/t starts); by n=4 it's landed on Benford's curve almost exactly — convergence rate matches a closed-form prediction (𝓡^n) from Day 3's own number. TV distance: 0.310 → 0.082 → 0.022 → 0.008 against predicted 0.309/0.095/0.030/0.009. Caption in `benford_law/lingua/02_cascade_post.md`. |
| 6 | A-story | When two burdens land on the same block — or don't | transit_pollution_overlap.ipynb | READY | Population-weighted Day 2's block-group transit gap score up to tract level (GEOID prefix match, no spatial join needed), scatter it against Day 4's tract-level env burden composite. Hypothesis going in was compounding burden; the real number is a weak but statistically real trade-off instead (Spearman rho=-0.24, p=0.002) — worst-transit tracts skew toward *lower* pollution, not higher. Reads as urban/rural split: I-5 corridor has decent transit but worse traffic/diesel; rural tracts have poor transit but cleaner air. Caption in `transit_pollution_overlap_post.md`. |
| 7 | B-story | Benford's Law with words, part 3: five separate languages, one seeming center of gravity | ../benford_law/lingua/notebooks/wordcloud_languages.ipynb | READY — 2 images | **Replaces the old "Five registers" plan (03_register.ipynb) — Simon's call 2026-08-14: register comparison is "bleh," same-language texts obviously cluster on the same common words, not an interesting reason for a cloud to look dense.** New approach, no bar/dot/pie charts from here on — the circle itself is the chart. Same New Testament text, originally English/Greek/Hebrew/Russian/Hindi; **language set changed 2026-08-15** — swapped Russian to Estonian and Hindi to Czech, two of this project's own known high-𝓡 outliers (0.69/0.66 vs the original set's narrow 0.23-0.51 band), for a genuinely wider spread. Final five: English (𝓡=0.437), Greek (0.511), Hebrew (0.227), Estonian (0.693), Czech (0.657) — Estonian/Czech both Latin script like English, so this is now three scripts (Latin/Greek/Hebrew), not five; title/copy changed from "scripts" to "languages" accordingly. Each language's real words placed at their own theta position, sized by real frequency. Second image: densest 90-degree wedge (57.0% of combined mass under the new set), data-driven crop, reprojected onto its own full circle. Real bug caught and fixed mid-build: matplotlib was silently dropping `fontsize` whenever `fontproperties` was also passed, so 4 of 5 languages were never actually sized by frequency until Simon asked why English looked bigger. **A cascade-convergence GIF was also built on top of this piece (same 5 languages) but reserved out, not posted here — see Day 9, which builds its own GIF against a different, purpose-matched language set instead of reusing this one.** Caption in `benford_law/lingua/wordcloud_languages_post.md`, ends with a teaser question leading into Day 11 (which uses Estonian/Finnish/Hungarian — Estonian now does double duty, appearing in both pieces for different reasons). `03_register.ipynb` still exists and still works, just not used for this slot. **Corpus-vintage note (2026-08-15, Simon's catch):** Estonian's source here is the 1739 Anton thor Helle translation (Baltic-German pastor, colonial-era) and Czech's is the 1613 Bible Kralická — both far older than the other three languages' sources. Considered swapping Estonian for Finnish (modern 1933 translation, already vetted elsewhere in this project) to fix this, but reverted — keeping Estonian. Decided not to chase a modern Czech translation either ("too much work, we're not doing actual research") — stated as a corpus limitation in the caption instead of silently accepted. |
| 8 | A-story | How far is a ballot drop box | ballot_access_snohomish.ipynb | READY | Election-season piece: 858 Snohomish County precincts (Nov 2024 general, last completed cycle), distance from precinct centroid to nearest of 36 official drop boxes (0.02-12.6 mi). Two-level finding, and the levels disagree: raw ballots cast per precinct shows no relationship (r=-0.05, n=858) — but that metric conflates population size with engagement. Stepping up to the only granularity with a real registered-voter denominator (20 cities, via the county's own validation report) shows a moderate relationship instead (r=-0.38) that doesn't clear significance at that n (p=0.10). Reported as "worth watching, not proven," not resolved into a clean headline. Real join headache: results.vote.wa.gov's precinct codes, the county GIS precinct layer's codes, and the city-level validation report all use unrelated ID systems across three different sources; precinct join done on normalized **name** (858/871 matched), city-level registration data recovered via a Wayback Machine snapshot since the county overwrites that report's URL annually. Caption in `ballot_access_snohomish_post.md`. |
| 9 | B-story (5/7) | Same finish line, different distance | ../benford_law/lingua/notebooks/05_language_cascade.ipynb | READY — grid + GIF | **Swapped into Day 9 from Day 11 on 2026-08-15 (Simon's call)** — Day 7 introduces Estonian, this piece introduces Finnish (setting up Day 11's Finnish/Estonian/Hungarian payoff), Day 11 now resolves it. The language-family sweep (`04_languages.ipynb`) spans 𝓡=0.28 to 0.74, so fixed n=1-4 doesn't fit everyone. Four languages (Hebrew Masoretic/Spanish/Czech/Finnish, OT), each given its own stage count so the last panel lands at the same ~1% closeness to Benford — Hebrew/Spanish need 4 multiplications, Czech needs 10, Finnish needs 14. One number (𝓡) predicts exactly how many. **GIF companion added 2026-08-15**: `img/05_language_cascade.gif`, 4 frames — NOT the same fixed-n-per-frame approach as Day 7's GIF; each frame plays each language's own `stages[i]`, so frame 1 is n=1 for everyone but frame 4 is Hebrew's 4th multiplication next to Finnish's 14th, landing at nearly the same converged circle (R=0.00-0.02) despite the wildly different n — makes the grid's static "same finish line" claim into a live visual. Built against this notebook's own language set (not reused from Day 7, which uses a different 5 languages/NT texts) so the grid and GIF are genuinely showing the same underlying data. Self-check confirms empirical R(n) matches predicted R(1)**n within 0.03. Radius scale recomputed per-frame, same fix as Day 7's GIF needed. |
| 10 | A-story | How far is a ballot drop box — statewide | ballot_access_statewide.ipynb | READY | Scaled Day 8 up to all 39 WA counties: real official turnout (65.0-87.7%, results.vote.wa.gov/Turnout.html, no aggregation needed) vs. distance to nearest of 573 statewide drop boxes. Building it at this scale exposed a real bug, not just a sample-size fix: a geometric county centroid (same technique Day 8 used at precinct scale) put Jefferson/Whatcom/King counties' reference points 40-48 miles into uninhabited wilderness, giving a meaningless r=0.03. Fixed with WA OFM's 2024 population estimates by census block group (5,311 statewide), population-weighted per county — corrected result is r=-0.18, p=0.26 (still not significant at n=39, but directionally consistent and no longer noise). Both numbers reported on purpose: the broken run is evidence the fix mattered, not a discarded draft. Third and final granularity in this sub-thread (precinct null -> city-level suggestive/underpowered -> statewide weak/corrected) reads as "probably something small, not proven." Caption in `ballot_access_statewide_post.md`. |
| 11 | B-story | Core vocabulary vs. the rare tail | ../benford_law/lingua/notebooks/core_vs_tail_vocabulary.ipynb | IN PROGRESS — image built, caption not started | **Swapped into Day 11 from Day 9 on 2026-08-15 (Simon's call)** — pays off the Finnish/Estonian/Hungarian question Day 7 dangles and Day 9 (now the cascade grid+GIF) reinforces by introducing Finnish. **Replaces the old "Twenty-five languages" dot-chart plan (04_languages.ipynb) — Simon's call 2026-08-14, build around the sharper finding below instead.** Real investigation, several dead ends caught before building anything: NOT the shared cognate *ja* (removing it from Finnish/Estonian barely moves 𝓡, 0.7%/-0.4% relative); NOT the shared letter either (removing every single j-initial word, ~12% of tokens, still barely moves it). What actually explains it: splitting each language's vocabulary into its top-100 most-frequent "core" words vs. everything else ("tail," mostly rare inflected forms) shows an inversion — Finnish/Estonian's core is *more* internally consistent than their tail (R_core 0.81/0.74 > R_tail 0.71/0.67), Hungarian's is the reverse (R_core 0.33 < R_tail 0.51). Image built: `img/core_vs_tail_vocabulary.png`, one circle per language, core words as real legible text (Day 7's technique) over the tail's density silhouette (theta_circle's technique), solid arrow = core resultant, dashed = tail resultant. A separate real anomaly was investigated and deliberately dropped from the piece per Simon's explicit call ("sick of caveats, just drop it") — Estonian's tail has a suspiciously low type-token ratio (0.011 vs Finnish 0.104/Hungarian 0.113), most likely because its source text is a 1739 translation vs. Finnish's 1933 and Hungarian's 1908, not a grammatical fact — this claim never made it into the image or any caption, so there's nothing to walk back. (This 1739-translation fact is now also stated directly, not just investigated-and-dropped, in Day 7's own caption as a corpus-vintage limitation — no longer just an internal note.) Caption not yet drafted; legibility of the crowded bottom of each circle (100 words is more than Day 7's ~25-30) was flagged but left as-is pending Simon's call. |
| 12 | Filler (math) | What is multiplication actually doing? | shape_multiplication.ipynb | READY — real Cartesian-product construction (`{(a,b): a in A, b in B}`), not a surface-of-revolution shortcut. Grew from the `kb/drawing_campaign_multiplying_shapes` idea (Eugenia Cheng's *Is Math Real?*). Checked numerically before any render: measure multiplies (`Area x Length = Volume`, two independent constructions agree with the analytic value to 0.4%/0.007%), commutativity is axis-relabeling (`A x B` and `B x A` produce the identical point set once columns are permuted), and hollow vs. filled survives the operation in a way a bare number can't — a hollow circle x segment gives a hollow cylinder (still intrinsically flat, same as a square, just rolled up), a filled disk x segment gives a genuinely solid one. Extended same session on Simon's "star times square?": two already-2D shapes multiply to a real 4D solid (a duoprism, `dim(star)+dim(square)=4`) that can't be rendered directly — hypervolume-multiplies check still passes in R^4 (Monte Carlo vs. `Area(star)*Area(square)`, 0.008% error) — so what's shown is its rotating shadow instead, the standard tesseract-projection trick (40 vertices as literal `(star_i, square_j)` pairs, 80 edges in two families). `shape_multiplication_dimensions.png` (hollow/filled/square comparison), `shape_multiplication_build.gif` (70-frame sweep), `shape_multiplication_duoprism.gif` (90-frame rotating shadow). Caption in `shape_multiplication_post.md` went through two rewrites — first pass was too technical ("Cartesian product," "dim(A)+dim(B)" leading the post, Simon: "WTF is going on here... just confusing"), rewritten in plain language, then Simon wrote his own final pass in first person and asked for the circle/disk distinction made explicit ("hollow vs filled circle makes a hollow vs filled cylinder") and the NOTES section added. |
| TBD | **Unslotted, displaced from Day 12** | The desert campus isn't the burden hotspot | dc_ej_water_tradeoff.ipynb | READY | Merascope backlog piece (EJ/community burden from DC clustering, drawn down from the backlog list above). Every confirmed, active WA data-center site matched by exact point-in-polygon to its real ZCTA in merascope's own pipeline, plotting `ej_score` against `water_score` (real indicators, not an invented composite). Going-in assumption was that the water-scarce desert campuses (Quincy) would also be the EJ hotspots — they're not. Tukwila (2 confirmed sites) has the best water access of any WA cluster and the worst EJ score; Quincy (3 confirmed sites, driest water score in the state) reads as *less* EJ-burdened. Self-check (assert-based, in-notebook) confirms the reversal before any claim is made. n=5 city clusters — too small for a correlation stat, reported as a direct descriptive reversal. Caption in `dc_ej_water_tradeoff_post.md`. **Not the reserved fire-risk finding** (that one — Microsoft EAT06/EAT09 inside the 2005 Badger Road perimeter — stays held per the "Do not use" list below; this piece uses `ej_score`/`water_score`, untouched by Stiffler or the fire-risk hold). |
| 13 | B-story (6/7) | Same words, same math, only the labels changed | ../benford_law/lingua/notebooks/06_ordering_sweep.ipynb | READY | Closes the loop to Day 3: sort the alphabet by word-initial frequency (best case) vs. reverse (worst case), measure 𝓡 under both, for 5 languages. Frequency-descending is a near-universal floor (0.17-0.36 for all). Frequency-ascending is not universal — English needs 16 multiplications, Czech needs 53. Day 3's "our alphabet sits close to the friendly end" holds for English/Hebrew/Spanish but not Czech/Finnish. |
| 15 | B-story (7/7) | What the best/worst-case gap is actually measuring | ../benford_law/lingua/notebooks/07_spread_gini.ipynb | READY | Day 13's best/worst gap varies hugely by language. Across 15 letter-alphabet languages, that gap tracks the Gini coefficient of first-letter frequency (rho=0.82, p<0.001) far better than raw alphabet size (rho=0.63). Devanagari's 1000+ symbol "alphabets" sit off-scale, same direction, more extreme (Gini 0.93-0.96). Stated in the piece: this confirms a mechanism, not an independent discovery. |
| TBD | **Thread/day unassigned — Simon's call** | The tool the job postings want couldn't hold my own data | canopy_semantic_layer.ipynb | READY — 2 PNGs + 1 GIF | New thread, 2026-08-18: not A-story (not civic data) and not really Filler (real findings, not generative art) — meta piece about tool choice itself. Power BI Service blocked personal-email signup; Looker Studio fallback worked until its map chart crashed the browser at 32,978 markers (this project's actual `GrapeExpectations` grid, WA wine country). Pivoted back to matplotlib, but framed as a "semantic layer" (`_mapping.py`, GrapeExpectations `powerbi_dashboard/`) encoding decisions Simon specified and Claude implemented: triangle-masked `tricontourf` instead of a naive fill that smears color across gaps between disconnected vineyard blocks; diverging vs. sequential color tied to whether a metric is a signed delta or a magnitude (first pass reused the wrong ramp, declining cells rendered as if they were bare ground); a shared peak/trough date instead of a per-cell argmax (per-cell version was badly speckled — noise from independent per-cell date selection, not signal). Three pieces: build-up (bud break to pre-harvest peak, mean +0.22 NDVI), decline (peak to harvest, mean -0.07, expected for wine grapes — deficit irrigation after veraison), and the full season as a 54-frame GIF. Self-check in-notebook: the same block lags in build-up AND fails to decline in the second map — Pearson r=-0.606 across all 32,978 cells confirms this is one pattern, not two maps that happen to look similar. Explicit framing point of the piece (Simon's ask, 2026-08-18): credit the specification and the catches, not "AI generated the code and images." Caption in `canopy_semantic_layer_post.md`, day number placeholder pending slotting.

**A-story backlog (not yet dated), from merascope** — indicators never touched by the Stiffler press channel, safe to post:
- **Seismic risk across WA DC clusters** — Liberty Lake reads 1.000 (best), contrast against other clusters. USGS hazard data, untouched by any report or tip.

Odd-day filler resumes at Day 17 once B-story completes: storm (fate_motif_storm.ipynb), gallop (william_tell_gallop.ipynb), 1812 (overture_1812_finale.ipynb), phase space (phase_space_flow.ipynb), quasicrystal (quasicrystal_field.ipynb), interference (interference_symmetry_science.ipynb), heat equation (heat_equation_flow.ipynb, built 2026-08-14 — 3 GIFs: shapes diffusing by curvature, a moving Lissajous heat source leaving a comet trail, three boundary conditions compared) — all READY, reframe/caption pass only, order not yet decided.

## Do not use — reserved for other channels

- **Superfund/RCRA proximity** and **interconnection-queue/grid-capacity** (merascope `contamination_score`-adjacent + `grid_capacity_score`) — named in `project_datacenter_siting` memory as the next two press-tip candidates for Lisa Stiffler. Posting these on LinkedIn first burns the exclusivity that makes a tip worth sending her.
- **Fire-risk report** (Microsoft's EAT06/EAT09 campus inside a documented 2005 fire perimeter) — built and tested, never sent to Stiffler, but decided 2026-08-11 to hold for a possible future press tip rather than post on LinkedIn. Same caliber finding as the water-stress tip; don't spend it here.
- **speed_zone_audit** (Lynnwood) — decided 2026-08-11 to hold until the pending councilmember pitch happens or is dropped, not to post to LinkedIn first.

## Risks

- Geography for A-story has defaulted to Snohomish County (snotrac/envburden) plus WA statewide (merascope) — redirect if a different county/topic is wanted for the next piece.
- housing_commute_burden.ipynb (housing cost vs. commute time, WA metro) is still unscoped and no longer on the calendar — pick back up if the merascope backlog above runs out before new data gets sourced for it.

## Distribution note (2026-08-15)

Days 1-4 impressions were flat (207/200/210/195) but days 5-7 cliffed (111/46/27) with daily posting continuing throughout. Engagement has been near-nil on every post since day 1 (best days: 1 comment + 2 likes, wife's like included) — this rules out an "AI slop" content-moderation flag (that would suppress reach on the flagged post specifically, not compound across posts; Simon's own likes/comments on other people's posts land normally, so the account isn't restricted). Working hypothesis instead: LinkedIn's account-level distribution throttle, triggered by sustained near-zero engagement plus daily cadence reading as spam-adjacent.

**Not yet resolved:** raw impression totals aren't age-adjusted — day 1 had 6 days to accumulate by the time it was checked, day 7 had ~0. `linkedin_engagement_tracker.ipynb` tracks impressions/day-since-posted, the metric that actually separates "throttled" from "hasn't finished its normal decay curve yet." That notebook's seed `post_date` values for days 4-7 are inferred from file mtimes (assumed daily 08-12 through 08-15) — confirm/correct against real post times before trusting the trend.

Cadence changed 2026-08-15: daily -> every 3-5 days, to break the feedback loop regardless of which hypothesis holds. Log a reading in the tracker daily either way.

## Notes
- Extend this table as new ideas surface; keep status current.
